const layoutProps = {
  config: {
    type: Object,
    default: () => ({})
  },
  fieldKey: {
    type: String
  },
  model: {
    type: Object,
    default: () => ({})
  }
};

export { layoutProps };
